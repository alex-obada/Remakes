# To doo
* make ```cox::vector```
* update ```cox::string```
* ```cox::unique_ptr``` custom deleter
* custom allocator
* finish ```cox::matrix``` (maybe use ```cox::vector``` instead of ```T**```
